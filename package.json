{
  "name": "disting",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "fuse.js": "^3.2.0",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build  && cd build/ && git init && git add . && git commit -m 'Build' && git remote add origin git@github.com:denimandsteel/disting.git && git push -f origin master:gh-pages",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
